---
name: bilingual-paper-pdf
description: 生产论文级中英双语对照 PDF（左英右中、同页镜像、"就是这篇论文的中文版"的观感）。只要用户想翻译学术论文并保持原版排版、做双语对照版/对照PDF/中英对照、提到"像智谱4Flash那个双语版一样"、或要求译文镜像对准原文, 就用这个 skill。支持两种模式：有参考样例 PDF 时逐像素复刻其工艺; 没有参考版时用同一套工艺从零生产。包含字体配置、差分分类、逐行精排、像素级审核全套脚本。
---

# 双语对照论文 PDF 生产工艺

目标产物：每个跨页 1224×792，左半 = 英文原页矢量原样，右半 = 英文原页整页克隆 + 抹掉纯文字 + 中文排版进去。图、公式、表格、脚注横线、arXiv 侧边印章**分毫不差**（它们是克隆带来的原物），中文"住"进原文文字腾出来的空间里，每段起点对准左页对应英文段。

## 铁律 (违反任何一条 = 返工)

1. **绝不裁剪/复制参考版或原文页的像素与文字对象**。只允许消费"文字 + 坐标"这类事实，每一个字形都必须由脚本从字体文件现画（insert_text）。
2. **先量后做**：字号、行距、缩进、栏位、边距全部从 PDF 元数据实测，禁止目测估计。
3. **审核不过不得交付**：两遍审核（逐页全检 + 差异聚类逐一放大定性）是交付门槛，机器差分只负责定位，定性必须人眼看图。
4. **动工前先读 [references/pitfalls.md](references/pitfalls.md)**，每一条都是真实返工换来的。

## 第〇步：环境

```bash
python <skill目录>/scripts/setup_fonts.py     # 装字体 + 生成字体测试卡
```
然后**打开 `fonts/fonts_test.pdf` 用眼睛确认每个字形正确**（错字/豆腐块 = 字体坏了，见 pitfalls 第1条）。
依赖：`pip install pymupdf fonttools pillow numpy`。

## 模式选择

| 情形 | 模式 |
|---|---|
| 用户给了参考样例（别人做好的双语版 PDF） | **模式A：复刻参考版工艺**（质量上限，推荐） |
| 只有英文原文 PDF | **模式B：同一工艺从零生产** |

## 模式A：有参考版

### A1. 逆向参考版工艺 (动手前必须完成, 结论写下来)

用 PyMuPDF 实测并回答：
- 字号/行距/首行缩进/栏位/边距（span 元数据 + 行坐标统计）
- 右半是不是整页克隆？决定性物证：找左页边缘的竖排印章/页边注在右半是否同坐标出现
- 参考版用的字体（span 的 font 字段就是配料表）
- 右页有无页眉页码、有无分隔线、参考文献在不在范围

### A2. 差分分类 + 构建

```bash
python scripts/prep_reference.py --src 原文.pdf --ref 参考版.pdf --out plan.json
python scripts/build_from_plan.py --src 原文.pdf --plan plan.json --out 双语版.pdf
```

prep 的原理（枢纽）：对英文原页每个文字行，到参考版右半同一坐标找——同位置同文字=保留（克隆物），同位置没文字=抹白（腾给中文），同位置不同文字=抹白+由参考版中文行接管。参考版自己排的中文行（CJK 占比>0.25）全部按原坐标输出，构建时逐行精排：行按参考版 span 原点落位，行内两端对齐由我的字距计算把行撑到参考版行宽。

跑完检查 prep 报告：keep 应集中在图/公式/表格区；anomaly 应为图注正文重叠；若表格数字出现在 anomaly 里 = 有 bug。

### A3. 两遍审核

```bash
python scripts/audit.py --mine 双语版.pdf --ref 参考版.pdf --outdir _audit
```
- **第一遍**：逐页看 `_audit/mine_pN.png` vs `ref_pN.png` 全页对照
- **第二遍**：逐个打开 `zooms_pN.png`（差异聚类对照条，上=我的输出，下=参考版），**每一个都要定性**：内容级差异（必改）还是亚像素渲染漂移（可接受）
- 差分数据参考基线：纯文字页 diff ≤ 0.05%，图表 overlay 页 ≤ 0.2%，混排密集首页 ≤ 0.7%。明显高于基线 = 有真问题
- 发现问题 → 修 → 重建 → **重新跑完整两遍审核**

## 模式B：无参考版

同一工艺，锚点自己生成，译文自己写。**质量上限与模式A相同**，差别仅在审核从"对照参考版"变为"自洽性校验"。

### B1. 生成翻译工作表

```bash
python scripts/solo_worksheet.py --src 原文.pdf --out worksheet.json     [--zones zones.json] [--math-exclude CMR,CMBX,CMTI,CMSS,CMTT,SF]
```
脚本自动分类：印章/插图/表格区/独立公式行 → 保留（克隆原样）；其余正文 → 待翻译槽位（记录页/栏/锚点y/可用高度/英文原文）。

两个重要参数（换新论文时按 pitfalls 39–41 判断）：
- `--math-exclude`：**正文本身就是 Computer Modern 家族时必填**（Springer/LaTeX 正文 CMR10）。
  否则每个正文行都会被判成"数学行"而漏翻。
- `--zones`：自动检测漏掉的保留区（只有横线的书板表格、拼图式插图、曲线型结果图）。
  用"图注锚定法"生成（见 references/pitfalls.md 第40条），格式
  `[{"page":7,"x0":…,"y0":…,"x1":…,"y1":…}]`。构建时同一份数据放进 content 的
  `extra_zones`，两侧分类才会一致。

**逐页核对保留分类**（对照原 PDF）：公式被抹了？图内标签被当正文了？表格单元格被当正文了？
分类错了回到 pitfalls 第14、39–41条调条件。**渲染一张打框对照图逐页目检是最快的核对方式**
（保留区红框、槽位蓝框叠在原页上）。

### B2. 翻译（质量核心，认真对待）

复制 worksheet.json 为 content.json，按 **[references/content-format.md](references/content-format.md)** 填 `cn` 字段。
硬性要求：先建术语表再动笔；术语全文一致；人名不臆造；粗体对齐；引用数字原样；图表内部文字用 overlays 汉化且只动英文标签行。

### B3. 构建

```bash
python scripts/build_solo.py --src 原文.pdf --content content.json --out 双语版.pdf
```
每段中文起点 = 左页对应英文段起点（±2pt 工艺精度），中英长度差用段内行距梯子（1.50→1.30，再缩字号 9.5）吸收，行内两端对齐。**警告输出（行距压底/溢出/空槽位）必须处理完再进入审核。**

作者-年份引用风格的论文（正文引用形如 (Surname et al., 2020)）构建后补一步右半引用跳转：
`python scripts/links_ay.py 双语版.pdf`（数字编号式 [n] 无需此步，linkify 已处理；条目分词/字体层规则见 pitfalls 60 一带的批量经验）。

### B4. 审核

```bash
python scripts/audit.py --mine 双语版.pdf --outdir _audit
```
逐页目检：无豆腐块、无英文残留、无叠印、无溢出、图表公式完整、每段中文起点贴着英文段起点。再抽 2-3 页和原 PDF 并排对照版式气质（字号/行距/缩进/对齐）。

## 交付前清理

- 文件偏大（完整字体内嵌，~40MB）属正常；要压缩用 fontTools 对实际用到的码点子集化（**不要用 `doc.subset_fonts()`**，见 pitfalls 第3条）
- 交付前跑机器门禁（7 项，全过才可交付）：
  `python scripts/check_release.py --src 原文.pdf --content content.json --pdf 成品.pdf --build-log build.log`
  （标记配对/槽位覆盖/中文叠印/中文×保留英文/缺字形/链接数/日志无警告；日志重定向见 pitfalls 61；
  门禁盲区见 pitfalls 58——溢出跨栏的叠印要另用分词宽度扫描器排查）
- 视觉审核用 judge agent 逐页过，每个 fail 都要定位到坐标再修（坐标证据判定，防误报）；
  **重建后必须立即重渲审核图**（pitfalls 60，旧图会误导判）
- 报告里如实说明：哪些内容级差异已修为零、残留的渲染级漂移在哪几页、参考版/译文的已知瑕疵（如原文自身的排版毛病不要替用户"顺手修掉"，要先提出）

## 换新论文时的注意点

- 图表区自动检测（模式B）对密集小图页可能误判，必须逐页人工核对分类报告
- 参考版若非"整页克隆"工艺（如纯重排版），模式A的差分分类仍然成立，但 keep 会很少——如实向用户说明差异
- 单栏论文：栏位逻辑按单栏走（col_edges 已按列统计，天然适配）
